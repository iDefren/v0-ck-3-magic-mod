# ============================================================
# ARCANE DOMINION - common/decisions/02_laboratory_decisions.txt
# Laboratory Decisions
# ============================================================

decisions = {

# Establish Magic Laboratory
establish_laboratory = {
	title = establish_laboratory_title
	desc = establish_laboratory_desc
	picture = "gfx/interface/illustrations/decisions/decision_laboratory.dds"
	selection_tooltip = establish_laboratory_tooltip

	is_shown = {
		OR = {
			has_trait = mage_adept
			has_trait = mage_journeyman
			has_trait = mage_expert
			has_trait = mage_master
			has_trait = archmage
		}
		has_variable = arcane_mana
		var:arcane_mana >= 10
		is_landed = yes
		NOT = { has_character_flag = laboratory_established }
	}

	is_valid = {
		var:arcane_mana >= 10
		is_capable_adult = yes
		gold >= 200
	}

	effect = {
		change_variable = { name = arcane_mana add = -10 }
		remove_short_term_gold = 200
		set_character_flag = laboratory_established
		add_character_modifier = {
			modifier = laboratory_owner
			years = -1
		}
		trigger_event = laboratory_events.0001
	}

	ai_check_interval = 60
	ai_potential = {
		has_trait = scholar
	}
}

# Conduct Research
conduct_research = {
	title = conduct_research_title
	desc = conduct_research_desc
	picture = "gfx/interface/illustrations/decisions/decision_research.dds"
	selection_tooltip = conduct_research_tooltip

	is_shown = {
		has_character_flag = laboratory_established
		has_variable = arcane_mana
		var:arcane_mana >= 15
	}

	is_valid = {
		var:arcane_mana >= 15
		learning >= 12
		is_capable_adult = yes
	}

	effect = {
		change_variable = { name = arcane_mana add = -15 }
		change_variable = { name = arcane_mana add = 25 }
		add_character_modifier = {
			modifier = arcane_research_boost
			years = 3
		}
		trigger_event = laboratory_events.0010
	}

	ai_check_interval = 48
	ai_potential = {
		has_trait = scholar
	}
}

# Magical Experimentation
magical_experimentation = {
	title = magical_experimentation_title
	desc = magical_experimentation_desc
	picture = "gfx/interface/illustrations/decisions/decision_experimentation.dds"
	selection_tooltip = magical_experimentation_tooltip

	is_shown = {
		has_character_flag = laboratory_established
		has_variable = arcane_mana
		var:arcane_mana >= 20
	}

	is_valid = {
		var:arcane_mana >= 20
		is_capable_adult = yes
	}

	effect = {
		change_variable = { name = arcane_mana add = -20 }
		trigger_event = laboratory_events.0020
	}

	ai_check_interval = 60
	ai_potential = {
		NOT = { has_trait = craven }
	}
}

# Create Magical Artifact
create_artifact = {
	title = create_artifact_title
	desc = create_artifact_desc
	picture = "gfx/interface/illustrations/decisions/decision_artifact.dds"
	selection_tooltip = create_artifact_tooltip

	is_shown = {
		has_character_flag = laboratory_established
		OR = {
			has_trait = mage_expert
			has_trait = mage_master
			has_trait = archmage
		}
		has_variable = arcane_mana
		var:arcane_mana >= 40
	}

	is_valid = {
		var:arcane_mana >= 40
		learning >= 15
		is_capable_adult = yes
		gold >= 300
	}

	effect = {
		change_variable = { name = arcane_mana add = -40 }
		remove_short_term_gold = 300
		add_character_modifier = {
			modifier = artifact_crafter
			years = 10
		}
		trigger_event = artifact_events.0001
	}

	ai_check_interval = 120
	ai_potential = {
		has_trait = scholar
	}
}

# Enchant Item
enchant_item = {
	title = enchant_item_title
	desc = enchant_item_desc
	picture = "gfx/interface/illustrations/decisions/decision_enchant.dds"
	selection_tooltip = enchant_item_tooltip

	is_shown = {
		has_character_flag = laboratory_established
		has_variable = arcane_mana
		var:arcane_mana >= 15
	}

	is_valid = {
		var:arcane_mana >= 15
		learning >= 10
		is_capable_adult = yes
	}

	effect = {
		change_variable = { name = arcane_mana add = -15 }
		add_prestige = 20
		trigger_event = laboratory_events.0030
	}

	ai_check_interval = 72
	ai_potential = {
		has_trait = scholar
	}
}

# Brew Magical Potion
brew_potion = {
	title = brew_potion_title
	desc = brew_potion_desc
	picture = "gfx/interface/illustrations/decisions/decision_potion.dds"
	selection_tooltip = brew_potion_tooltip

	is_shown = {
		has_character_flag = laboratory_established
		has_variable = arcane_mana
		var:arcane_mana >= 10
	}

	is_valid = {
		var:arcane_mana >= 10
		learning >= 8
		is_capable_adult = yes
	}

	effect = {
		change_variable = { name = arcane_mana add = -10 }
		add_character_modifier = {
			modifier = potion_effects
			years = 2
		}
		trigger_event = laboratory_events.0040
	}

	ai_check_interval = 48
	ai_potential = {
		has_trait = scholar
	}
}

} # End of decisions block
